{
  "name": "parquet-view",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "./scripts/dev_server.js",
    "build-ui": "./scripts/build-ui",
    "build-wasm": "wasm-pack build --out-dir www/build/wasm-pkg .. -- -F console_error_panic_hook",
    "clean": "rm -rf build dist/{*.js,*.css,*wasm}"
  },
  "type": "module",
  "author": "mv",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "0.19.5",
    "esbuild-plugin-wasm": "^1.1.0"
  },
  "dependencies": {
    "elm-esm": "^1.1.4"
  }
}
